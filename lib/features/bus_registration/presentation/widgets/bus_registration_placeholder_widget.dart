import 'package:flutter/material.dart';

class BusRegistrationPlaceholderWidget extends StatelessWidget {
  const BusRegistrationPlaceholderWidget({super.key});
  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}

