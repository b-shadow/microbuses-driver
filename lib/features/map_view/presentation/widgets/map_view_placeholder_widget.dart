import 'package:flutter/material.dart';

class MapViewPlaceholderWidget extends StatelessWidget {
  const MapViewPlaceholderWidget({super.key});
  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}

