import 'package:flutter/material.dart';

class SettingsPlaceholderWidget extends StatelessWidget {
  const SettingsPlaceholderWidget({super.key});
  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}

