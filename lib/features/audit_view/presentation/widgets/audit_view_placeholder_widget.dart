import 'package:flutter/material.dart';

class AuditViewPlaceholderWidget extends StatelessWidget {
  const AuditViewPlaceholderWidget({super.key});
  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}

