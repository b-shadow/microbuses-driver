import 'package:flutter/material.dart';

class DriverProfilePlaceholderWidget extends StatelessWidget {
  const DriverProfilePlaceholderWidget({super.key});
  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}

