import 'package:flutter/material.dart';

class ActiveTripPlaceholderWidget extends StatelessWidget {
  const ActiveTripPlaceholderWidget({super.key});
  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}

