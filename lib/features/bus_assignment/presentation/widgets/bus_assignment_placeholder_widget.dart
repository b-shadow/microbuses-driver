import 'package:flutter/material.dart';

class BusAssignmentPlaceholderWidget extends StatelessWidget {
  const BusAssignmentPlaceholderWidget({super.key});
  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}

