import 'package:flutter/material.dart';

class TripStartPlaceholderWidget extends StatelessWidget {
  const TripStartPlaceholderWidget({super.key});
  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}

