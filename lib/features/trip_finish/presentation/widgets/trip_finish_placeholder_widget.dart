import 'package:flutter/material.dart';

class TripFinishPlaceholderWidget extends StatelessWidget {
  const TripFinishPlaceholderWidget({super.key});
  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}

