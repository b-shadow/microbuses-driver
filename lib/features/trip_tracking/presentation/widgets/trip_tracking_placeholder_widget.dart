import 'package:flutter/material.dart';

class TripTrackingPlaceholderWidget extends StatelessWidget {
  const TripTrackingPlaceholderWidget({super.key});
  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}

