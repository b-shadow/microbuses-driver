import 'package:flutter/material.dart';

class LocationSenderPlaceholderWidget extends StatelessWidget {
  const LocationSenderPlaceholderWidget({super.key});
  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}

