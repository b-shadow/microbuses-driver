import 'package:flutter/material.dart';

class LineSelectionPlaceholderWidget extends StatelessWidget {
  const LineSelectionPlaceholderWidget({super.key});
  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}

