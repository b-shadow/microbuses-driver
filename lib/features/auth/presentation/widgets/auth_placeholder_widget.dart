import 'package:flutter/material.dart';

class AuthPlaceholderWidget extends StatelessWidget {
  const AuthPlaceholderWidget({super.key});
  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}

